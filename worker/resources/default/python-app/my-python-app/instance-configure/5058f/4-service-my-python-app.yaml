apiVersion: v1
kind: Service
metadata:
  name: my-python-app
  namespace: default
  labels:
    app: my-python-app
    managed-by: kratix
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5000
    name: http
  selector:
    app: my-python-app
