apiVersion: v1
kind: Service
metadata:
  name: some-testing-app
  namespace: default
  labels:
    app: some-testing-app
    managed-by: kratix
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5000
    name: http
  selector:
    app: some-testing-app
