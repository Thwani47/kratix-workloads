apiVersion: v1
kind: Service
metadata:
  name: demo-test-app
  namespace: default
  labels:
    app: demo-test-app
    managed-by: kratix
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5000
    name: http
  selector:
    app: demo-test-app
