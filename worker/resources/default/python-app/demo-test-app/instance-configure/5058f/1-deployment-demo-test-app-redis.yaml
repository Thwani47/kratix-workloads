apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-test-app-redis
  namespace: default
  labels:
    app: demo-test-app-redis
    component: redis
    managed-by: kratix
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-test-app-redis
  template:
    metadata:
      labels:
        app: demo-test-app-redis
        component: redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
          name: redis
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi
        livenessProbe:
          tcpSocket:
            port: 6379
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - redis-cli
            - ping
          initialDelaySeconds: 5
          periodSeconds: 5
