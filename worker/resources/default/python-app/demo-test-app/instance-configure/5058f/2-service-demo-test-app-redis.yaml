apiVersion: v1
kind: Service
metadata:
  name: demo-test-app-redis
  namespace: default
  labels:
    app: demo-test-app-redis
    managed-by: kratix
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
    name: redis
  selector:
    app: demo-test-app-redis
