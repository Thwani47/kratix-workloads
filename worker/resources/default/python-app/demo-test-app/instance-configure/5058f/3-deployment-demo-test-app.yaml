apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-test-app
  namespace: default
  labels:
    app: demo-test-app
    environment: dev
    managed-by: kratix
  annotations:
    kratix.io/promise: python-app
    github.com/repository: https://github.com/thwani47/demo-test-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-test-app
  template:
    metadata:
      labels:
        app: demo-test-app
        environment: dev
    spec:
      containers:
      - name: demo-test-app
        image: ghcr.io/thwani47/demo-test-app:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
          name: http
        env:
        - name: PORT
          value: '5000'
        - name: ENVIRONMENT
          value: dev
        - name: REDIS_HOST
          value: demo-test-app-redis
        - name: REDIS_PORT
          value: '6379'
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 5
