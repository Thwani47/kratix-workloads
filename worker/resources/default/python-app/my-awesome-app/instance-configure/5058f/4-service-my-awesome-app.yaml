apiVersion: v1
kind: Service
metadata:
  name: my-awesome-app
  namespace: default
  labels:
    app: my-awesome-app
    managed-by: kratix
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5000
    name: http
  selector:
    app: my-awesome-app
