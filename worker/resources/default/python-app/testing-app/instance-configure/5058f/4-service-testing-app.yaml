apiVersion: v1
kind: Service
metadata:
  name: testing-app
  namespace: default
  labels:
    app: testing-app
    managed-by: kratix
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5000
    name: http
  selector:
    app: testing-app
