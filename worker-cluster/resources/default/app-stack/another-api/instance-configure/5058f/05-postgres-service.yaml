apiVersion: v1
kind: Service
metadata:
  name: another-api-postgres
  namespace: default
  labels:
    app: another-api-postgres
spec:
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: another-api-postgres
