apiVersion: v1
kind: Service
metadata:
  name: another-api-api
  namespace: default
  labels:
    app: another-api-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: another-api-api
