apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-stack-api-ingress
  namespace: default
  labels:
    app: test-stack-api
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  rules:
    - host: test-stack-api.local.gd
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: test-stack-api-api
                port:
                  number: 80
  tls:
    - hosts:
        - test-stack-api.local.gd
      secretName: test-stack-api-tls
