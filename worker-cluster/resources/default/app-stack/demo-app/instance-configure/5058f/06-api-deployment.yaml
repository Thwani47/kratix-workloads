apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app-api
  namespace: default
  labels:
    app: demo-app-api
    component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: demo-app-api
  template:
    metadata:
      labels:
        app: demo-app-api
        component: backend
    spec:
      containers:
        - name: api
          image: ghcr.io/thwani47/demo-api:latest
          ports:
            - containerPort: 8080
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: demo-app-db-credentials
                  key: database-url
            - name: DB_HOST
              value: demo-app-postgres
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: demo-app
            - name: DB_USER
              value: postgres
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: demo-app-db-credentials
                  key: postgres-password
            - name: ENVIRONMENT
              value: dev
            - name: PORT
              value: "8080"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
