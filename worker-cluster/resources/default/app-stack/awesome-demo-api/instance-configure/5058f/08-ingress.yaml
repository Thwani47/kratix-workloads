apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: awesome-demo-api-ingress
  namespace: default
  labels:
    app: awesome-demo-api
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  rules:
    - host: awesome-demo-api.local.gd
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: awesome-demo-api-api
                port:
                  number: 80
  tls:
    - hosts:
        - awesome-demo-api.local.gd
      secretName: awesome-demo-api-tls
