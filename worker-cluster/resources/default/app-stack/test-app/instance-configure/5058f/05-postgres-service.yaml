apiVersion: v1
kind: Service
metadata:
  name: test-app-postgres
  namespace: default
  labels:
    app: test-app-postgres
spec:
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: test-app-postgres
