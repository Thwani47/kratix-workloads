apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-app-postgres
  namespace: default
  labels:
    app: test-app-postgres
    component: database
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-app-postgres
  template:
    metadata:
      labels:
        app: test-app-postgres
        component: database
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: test-app
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: test-app-db-credentials
                  key: postgres-password
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: test-app-postgres-pvc
