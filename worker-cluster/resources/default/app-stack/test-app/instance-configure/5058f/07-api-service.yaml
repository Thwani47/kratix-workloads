apiVersion: v1
kind: Service
metadata:
  name: test-app-api
  namespace: default
  labels:
    app: test-app-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: test-app-api
