apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  creationTimestamp: null
  name: todoer
  namespace: default
spec:
  ingressClassName: nginx
  rules:
  - host: todoer.local.gd
    http:
      paths:
      - backend:
          service:
            name: todoer
            port:
              number: 8080
        path: /
        pathType: Prefix
status:
  loadBalancer: {}
